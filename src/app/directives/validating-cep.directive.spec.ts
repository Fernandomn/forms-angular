import { ValidatingCepDirective } from './validating-cep.directive';

describe('ValidatingCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatingCepDirective();
    expect(directive).toBeTruthy();
  });
});
