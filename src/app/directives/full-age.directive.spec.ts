import { FullAgeDirective } from './full-age.directive';

describe('FullAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new FullAgeDirective();
    expect(directive).toBeTruthy();
  });
});
